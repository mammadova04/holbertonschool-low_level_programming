#include <stdio.h>
#include "main.h"

/**
 * print_alphabet - prints the alphabet
 *
 * Return: Always 0 (Success)
 *
 */
void print_alphabet(void)
{
	char c;
	for (i = 'a'; i <= 'z'; i++)
	{
		_putchar(c);
	}
	_putchar('\n');
}
